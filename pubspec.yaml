name: globalscore_flutter
description: Plataforma de predicciones deportivas
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.1
  
  # Backend & Database
  supabase_flutter: ^1.10.25  # ← VERSIÓN ESTABLE SIN PROBLEMAS
  
  # Environment Variables
  flutter_dotenv: ^5.2.1
  
  # UI Components
  google_fonts: ^6.3.0
  intl: ^0.19.0
  
  # Icons
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  
  assets:
    - .env