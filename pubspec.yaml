name: globalscore_flutter
description: Plataforma de predicciones deportivas
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Backend
  supabase_flutter: ^2.5.0
  
  # State Management
  provider: ^6.1.0
  
  # Navegación
  go_router: ^13.2.0
  
  # UI/UX
  google_fonts: ^6.2.0
  flutter_svg: ^2.0.10
  cached_network_image: ^3.3.1
  
  # Utils
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  
  # Iconos
  lucide_icons_flutter: ^1.0.0
  
  # Variables de entorno
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # Cargar archivo .env
  assets:
    - .env